# vLLM 卡死问题调试日志使用说明

## 概述

为了帮助诊断vLLM请求卡死问题，我在关键位置添加了详细的调试日志。这些日志会记录：
- Block分配失败的原因
- Blocks释放情况
- 内存状态统计
- Preempt操作的详细信息

## 添加的日志位置

### 1. `vllm/v1/core/kv_cache_manager.py`

#### Block分配失败日志（WARNING级别）
- **位置**：`allocate_slots` 方法，当 `num_blocks_to_allocate > num_free_blocks` 时
- **内容**：
  - 请求ID
  - 需要的blocks数量
  - 可用的free blocks数量
  - 被缓存的blocks数量
  - 总blocks数量
  - 内存使用率
  - 详细的内存统计信息

#### Block分配异常日志（ERROR级别）
- **位置**：`allocate_slots` 方法，捕获 `ValueError` 异常时
- **内容**：
  - 异常信息
  - 请求ID
  - Free blocks数量
  - Cached blocks数量
  - 需要的blocks数量
  - 详细的内存统计信息

#### Block分配成功日志（DEBUG级别）
- **位置**：`allocate_slots` 方法，成功分配blocks后
- **内容**：
  - 请求ID
  - 分配的blocks数量
  - 分配后的free blocks数量

### 2. `vllm/v1/core/block_pool.py`

#### Blocks释放日志（DEBUG级别）
- **位置**：`free_blocks` 方法
- **内容**：
  - 释放的blocks总数
  - 被加入到free queue的blocks数量
  - 仍然在使用中的blocks数量（ref_cnt > 0）
  - 被缓存的blocks数量
  - 释放后的free blocks数量
  - ref_cnt分布统计（最小值、最大值、平均值）

#### Blocks未释放警告（WARNING级别）
- **位置**：`free_blocks` 方法，当有blocks的ref_cnt > 0时
- **内容**：
  - 未释放的blocks数量
  - 警告信息，提示可能存在引用计数问题

#### Free Blocks数量日志（DEBUG级别）
- **位置**：`get_num_free_blocks` 方法，当free blocks < 10%时
- **内容**：
  - Free blocks数量
  - 总blocks数量
  - Free blocks百分比
  - 被缓存的blocks数量
  - 内存使用率

#### 内存统计方法
- **位置**：新增 `get_memory_stats` 方法
- **返回内容**：
  ```python
  {
      "total_blocks": int,           # 总blocks数量（排除null block）
      "free_blocks": int,             # Free blocks数量
      "used_blocks": int,             # 使用的blocks数量
      "cached_blocks": int,           # 被缓存的blocks数量
      "cached_blocks_in_free_queue": int,  # 在free queue中但被缓存的blocks
      "usage_percent": float,         # 内存使用率（百分比）
      "ref_cnt_distribution": dict,   # ref_cnt分布 {ref_cnt: count}
  }
  ```

### 3. `vllm/v1/core/sched/scheduler.py`

#### Preempt操作日志（INFO级别）
- **位置**：调度循环中，preempt请求后
- **内容**：
  - 被preempt的请求ID
  - 目标请求ID
  - 释放的blocks数量
  - Preempt前后的free blocks数量

#### Preempt尝试日志（DEBUG级别）
- **位置**：调度循环中，block分配失败时
- **内容**：
  - 请求ID
  - Preempt尝试次数
  - 当前的free blocks数量

#### 超过最大Preempt尝试次数错误（ERROR级别）
- **位置**：调度循环中，当preempt尝试次数超过最大值时
- **内容**：
  - 最大尝试次数
  - 请求ID
  - Free blocks数量
  - 运行中的请求数量
  - 详细的内存统计信息

#### 调度失败错误（ERROR级别）
- **位置**：调度循环结束后，如果仍然无法分配blocks
- **内容**：
  - 请求ID
  - Preempt尝试次数
  - Free blocks数量
  - 运行中的请求数量
  - 等待中的请求数量
  - 详细的内存统计信息

## 如何使用这些日志

### 1. 启用日志

在运行vLLM时，设置适当的日志级别：

```bash
# 启用DEBUG级别日志（最详细）
export VLLM_LOGGING_LEVEL=DEBUG

# 或者使用Python代码
import logging
logging.basicConfig(level=logging.DEBUG)
```

### 2. 重现问题

使用 `qwen_trace_5min_sparse.jsonl` 重现卡死问题：

```python
# 运行你的测试脚本
python your_test_script.py --trace_file qwen_trace_5min_sparse.jsonl
```

### 3. 分析日志

当程序卡死时，查看日志文件，重点关注：

#### a) Block分配失败
查找包含 "Failed to allocate blocks" 的日志：
```
WARNING: Failed to allocate blocks for request <id>: need X blocks, but only Y free blocks available...
```
这会告诉你：
- 需要多少blocks
- 实际有多少free blocks
- 有多少被缓存的blocks
- 内存使用率
- ref_cnt分布

#### b) 异常信息
查找包含 "Exception during block allocation" 的日志：
```
ERROR: Exception during block allocation for request <id>: <exception message>...
```
这会告诉你分配过程中抛出的具体异常。

#### c) Preempt操作
查找包含 "Preempted request" 的日志：
```
INFO: Preempted request <id1> to free blocks for request <id2>. Freed X blocks...
```
这会告诉你：
- 哪些请求被preempt
- 释放了多少blocks
- Preempt是否有效

#### d) 超过最大尝试次数
查找包含 "Exceeded max preempt attempts" 的日志：
```
ERROR: Exceeded max preempt attempts...
```
这通常表示可能存在死锁或内存泄漏。

#### e) Blocks释放情况
查找包含 "Freed X blocks" 的日志：
```
DEBUG: Freed X blocks (Y added to free queue, Z still in use)...
```
这会告诉你：
- 释放了多少blocks
- 有多少被加入到free queue
- 有多少仍然在使用（可能存在问题）
- ref_cnt分布

### 4. 关键指标监控

在日志中关注以下关键指标：

1. **Free blocks数量趋势**
   - 是否持续下降？
   - 是否在某个值附近卡住？

2. **Cached blocks数量**
   - 是否持续增长？
   - 是否占用了大量内存？

3. **ref_cnt分布**
   - 是否有ref_cnt > 0但应该为0的blocks？
   - 是否有ref_cnt = 0但仍在使用的blocks？

4. **Preempt效果**
   - Preempt后free blocks是否增加？
   - 如果preempt后free blocks不增加，可能存在问题

### 5. 常见问题诊断

#### 问题1：Free blocks数量不准确
**症状**：日志显示有足够的free blocks，但分配失败
**可能原因**：
- Free queue状态不一致
- 被缓存的blocks需要先evict
**检查**：查看 `cached_blocks_in_free_queue` 的值

#### 问题2：Blocks无法释放
**症状**：日志显示 "X blocks were not freed (ref_cnt > 0)"
**可能原因**：
- 引用计数泄漏
- ref_cnt没有正确递减
**检查**：查看 `ref_cnt_distribution` 的详细信息

#### 问题3：Preempt无效
**症状**：Preempt后free blocks不增加
**可能原因**：
- Preempt的请求的blocks无法释放
- 内存泄漏
**检查**：查看preempt前后的free blocks数量对比

#### 问题4：超过最大尝试次数
**症状**：日志显示 "Exceeded max preempt attempts"
**可能原因**：
- 死锁
- 所有请求都无法被preempt
- 内存泄漏导致无法释放blocks
**检查**：查看详细的内存统计信息

## 日志级别建议

- **生产环境**：使用 WARNING 或 ERROR 级别，只记录重要问题
- **调试环境**：使用 DEBUG 级别，记录所有详细信息
- **性能测试**：使用 INFO 级别，记录关键操作但不影响性能

## 性能影响

- DEBUG级别日志：可能对性能有轻微影响，建议仅在调试时使用
- WARNING/ERROR级别日志：影响很小，可以长期启用
- 内存统计计算：仅在需要时计算（如分配失败时），不会影响正常流程

## 下一步

根据日志分析结果：
1. 如果发现ref_cnt问题，检查引用计数的维护逻辑
2. 如果发现free queue状态不一致，检查free queue的管理逻辑
3. 如果发现缓存blocks过多，考虑调整缓存策略或添加eviction机制
4. 如果发现异常未被捕获，添加相应的异常处理

