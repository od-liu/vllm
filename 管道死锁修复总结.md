# 管道死锁修复总结

## 问题诊断

通过分析日志发现，卡死问题**不是内存问题**，而是**管道阻塞死锁问题**：

### 关键发现：
1. **Free blocks 充足**：日志显示 free blocks 一直在 53,400+ 以上，内存资源非常充足
2. **Blocks 管理正常**：所有 blocks 的分配和释放都正常工作
3. **请求已完成但无法返回**：
   - EngineCore 已完成请求 59 并释放了 blocks（行557-558）
   - 但 trace_scheduler 持续输出 "Request 59 still running"（行564-567）
   - 第60个请求永远不会启动

### 死锁原因：
```
run_e2e_sweep.py          trace_scheduler.py
      |                         |
   阻塞在读取 stdout  <---死锁--->  heartbeat线程阻塞在写入stdout
      |                         |
   等待更多输出              stdout缓冲区满
```

**原因链条**：
1. `run_e2e_sweep.py` 使用 `for line in process.stdout` 同步阻塞读取
2. `trace_scheduler` 的 heartbeat 线程每30秒输出日志到 stdout
3. 当 stdout 缓冲区满时：
   - trace_scheduler 写入阻塞 → 无法完成请求返回
   - run_e2e_sweep.py 读取阻塞 → 等待更多输出
   - **双向死锁！**

## 修复方案（方案C）

### 1. 修复 run_e2e_sweep.py 的管道阻塞

**文件**: `benchmarks/e2e_sweep/run_e2e_sweep.py`

**改动**：
- 添加 `queue` 和 `threading` 导入
- 使用独立线程异步读取 stdout，避免主线程阻塞
- 使用 Queue 在线程间传递数据
- 添加超时机制检查进程状态

**关键代码**：
```python
# 创建队列和reader线程
output_queue = queue.Queue()

def reader_thread():
    try:
        for line in process.stdout:
            output_queue.put(('line', line))
    except Exception as e:
        output_queue.put(('error', str(e)))
    finally:
        output_queue.put(('done', None))

reader = threading.Thread(target=reader_thread, daemon=True)
reader.start()

# 从队列中非阻塞读取
while not done:
    try:
        msg_type, data = output_queue.get(timeout=1)
        # 处理数据...
    except queue.Empty:
        # 检查进程是否结束
        if process.poll() is not None:
            break
```

### 2. 修复 trace_scheduler.py 的 heartbeat 机制

**文件**: `vllm/profiler/trace_scheduler.py`

**改动1 - 将 heartbeat 输出到 stderr**：
- 避免与主输出（stdout）混合，防止 stdout 缓冲区满
- 使用 `sys.stderr.write()` 直接写入
- 添加 `sys.stderr.flush()` 确保立即输出

**改动2 - 添加 finally 块**：
- 确保 heartbeat 线程在任何情况下都能停止
- 从 try/except 块中移除 `heartbeat_stop.set()`
- 在 finally 块中统一处理停止逻辑
- 使用 `heartbeat_thread.join(timeout=1.0)` 等待线程优雅退出

**改动3 - 增加心跳间隔**：
- 从 30秒 增加到 60秒
- 减少日志输出频率，降低缓冲区压力

**关键代码**：
```python
def heartbeat_monitor():
    interval = 60  # 从30改为60
    # ...
    # 使用 stderr 而不是 logger.info
    sys.stderr.write(
        f"[DP{rank}] INFO {timestamp} trace_scheduler.py:283] "
        f"  ⏱ Request {trace_req.chat_id} still running "
        f"({elapsed:.0f}s / {timeout:.0f}s)...\n"
    )
    sys.stderr.flush()

# 添加 finally 块
try:
    result = await asyncio.wait_for(...)
    return result[0] if result else None
except asyncio.TimeoutError:
    logger.error(...)
    return None
except Exception as e:
    logger.error(...)
    return None
finally:
    # 确保heartbeat线程总是停止
    heartbeat_stop.set()
    heartbeat_thread.join(timeout=1.0)
```

## 预期效果

1. ✅ **消除管道死锁**：run_e2e_sweep.py 不会再阻塞在读取 stdout
2. ✅ **避免缓冲区满**：heartbeat 输出到 stderr，不会造成 stdout 缓冲区满
3. ✅ **可靠的资源清理**：heartbeat 线程在任何情况下都能正确停止
4. ✅ **解决卡死问题**：不再出现最后一个请求卡死的情况
5. ✅ **减少日志压力**：心跳间隔从30秒增加到60秒

## 测试建议

使用相同的 trace 文件重新运行测试：
```bash
python benchmarks/e2e_sweep/run_e2e_sweep.py \
    --trace-file qwen-bailian-usagetraces-anon/qwen_trace_5min_sparse.jsonl \
    ...
```

观察：
1. 所有60个请求是否都能正常完成
2. 不再出现 "Request 59 still running" 持续输出的情况
3. 程序能正常退出

## 修改的文件

1. `benchmarks/e2e_sweep/run_e2e_sweep.py`
   - 添加 queue 和 threading 导入
   - 修改 run_e2e_benchmark 函数使用非阻塞读取

2. `vllm/profiler/trace_scheduler.py`
   - 添加 os 和 sys 导入
   - 修改 heartbeat_monitor 使用 stderr 输出
   - 增加心跳间隔到60秒
   - 添加 finally 块确保线程清理

## 技术要点

### 为什么只有最后一个请求卡死？
- 前59个请求：heartbeat 在请求完成后立即停止，缓冲区来得及清空
- 第60个请求：如果正好在 generate 完成后、返回前，stdout 缓冲区满了，就会触发死锁

### 为什么 blocks 充足还会卡死？
- 这不是内存问题，而是**进程间通信（IPC）问题**
- Engine Core 已经完成工作，但无法将结果返回给 trace_scheduler
- trace_scheduler 被 stdout 写入阻塞，无法接收结果

### 非阻塞读取的优势
- 主线程不会卡在 `for line in stdout`
- 可以检查进程状态和超时
- 即使写入端阻塞，读取端仍然可以工作
- 避免了经典的管道死锁问题


